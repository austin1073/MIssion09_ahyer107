
@{
}

@model Book

<div class="card card-outline-primary m-1 p-1">
    <div class="bg-light p-1">
        <h4 class="lead fw-bold"> <!--TA: The lead tag makes these words slightly taller and then the bold tage makes them dark to stand out-->
            @Model.Title
            <span class="badge bg-info" style="float:right">
                <small class="text-decoration-line-through">@((Model.Price*1.2).ToString("c"))</small> <!-- TA: I used the line through modification here to make it look like like every book is on sale-->
                <small>@Model.Price.ToString("c")</small>
            </span>
        </h4>
    </div>
    <div class="card-text p-1">Author: @Model.Author</div>
    <div class="card-text p-1">Publisher: @Model.Publisher</div>
    <div class="card-text p-1">Classification: @Model.Classification</div>
    <div class="card-text p-1">Category: @Model.Category</div>
    <div class="card-text p-1">PageCount: @Model.PageCount</div>

    <form method="post" asp-page="/Cart">

        <input type="hidden" asp-for="@Model.BookId" />
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />

        <span class="card-text">
            <button type="submit" class="btn btn-success btn-sm float-end">Add to cart</button>
        </span>
    </form>

</div>